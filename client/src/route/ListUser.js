import { ListedView } from "../component/ListedView"
export const ListUser =()=>{
    return (<div>
        <ListedView><div>ListUser</div></ListedView>
                {/* <Drawer variant="permanent" open={open}>
                    <Toolbar
                        sx={{
                            display: 'flex',
                            alignItems: 'center',
                            justifyContent: 'flex-end',
                            px: [1],
                        }}
                    >
                        <IconButton onClick={toggleDrawer}>
                            <ChevronLeftIcon />
                        </IconButton>
                    </Toolbar>
                    <Divider />
                    <List component="nav">
                        <ListItems/>
                    </List>
                </Drawer> */}
    </div>)
}